# wiki-manager

A Python 3.11 CLI project managed by `uv` and built with `typer`.

## Setup

```bash
uv sync
```

## Usage

```bash
uv run wiki --help
uv run wiki --version
uv run wiki hello
```

## Install as a uv tool

```bash
uv tool install .
wiki --help
```
