# Agent Bridge 访问上下文

本上下文描述 Agent Bridge 如何识别调用者，以及业务资源在内部协作中的归属与可见范围。

## Language

**调用者（Actor）**:
发起 Agent Bridge 操作的内部用户，以统一账户系统的用户 ID 作为唯一身份。
_Avoid_: 前端用户、请求用户、Linux 用户

**用户目录（User Directory）**:
由维护管理员登记的稳定用户 ID 集合；用户可存在但暂未归属任何小组。
_Avoid_: 用户小组映射、成员列表

**数据组（Group）**:
内部用户当前所属的协作小组，也是私有业务资源的访问边界。
_Avoid_: 角色、部门、租户

**用户归属（User Membership）**:
用户与其当前数据组之间的可变关系；换组或取消归属只改变该关系，不删除用户。
_Avoid_: 用户、小组成员本身

**资源归属组（Owner Group）**:
业务资源创建时确定的所属数据组；创建者后来换组不会改变资源归属。
_Avoid_: 创建人、资源管理员

**组内资源（Group Resource）**:
仅资源归属组成员可以读取、使用和修改的业务资源。
_Avoid_: 私有资源、个人资源

**共享资源（Shared Resource）**:
所有已识别调用者均可读取和使用、但仍仅由资源归属组修改的业务资源或工作流产物。
_Avoid_: 公共资源、全局资源

**工作流产物（Workflow Artifact）**:
工作流执行形成、可被后续检索和复用的持久化交付物；产物可独立标记为共享，但其工作流定义、任务和运行过程仍属于资源归属组。
_Avoid_: 工作流日志、运行记录

**小组业务数据（Group-owned Data）**:
由某个数据组创建或运行产生、必须受资源归属组约束的数据；其子记录和运行产物继承根资源或运行的小组归属。
_Avoid_: 用户数据、租户数据

**全局系统资源（System Resource）**:
由平台统一维护、天然面向所有数据组生效且不表达某个小组业务内容的配置或定义。
_Avoid_: 共享资源、公共数据

**运行数据（Run Data）**:
一次工具、Agent 或自动化运行产生的日志、事件、输入输出与产物，创建时固化发起调用者或根业务资源的数据组。
_Avoid_: 系统日志、临时数据

**维护管理员（Maintenance Admin）**:
由 `server.toml` 的 `admins` 指定、用于故障处理和存量迁移的维护旁路身份。
_Avoid_: 超级用户、业务管理员
